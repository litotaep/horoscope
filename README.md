# Horoscope (Tkinter)

A simple desktop application built with Python and Tkinter.  
It fetches daily, tomorrow's, or weekly horoscope data from horo.mail.ru using web scraping and displays it inside a clean GUI with scrollable text.

---

## 🚀 How to Run

1. Install dependencies:

```sh
pip install -r requirements.txt
